---
title: "研究：建立模块与载具引用矩阵"
parent: "挖掘机拯救世界：从规格到 M0 可制作路线"
type: research
mode: AFK
status: resolved
triage: ready-for-agent
blocked_by: []
---

## Question

在 `F:\WanderburgAssetRipperExport` 的 AssetRipper 导出中，哪些 Prefab、脚本签名、序列化字段和资源引用可以可靠地组成模块、载具阶段、槽位、升级预览与主动/被动表现的关系矩阵？哪些关系仍然未知，必须保留为设计假设？

## Resolution criteria

- 输出可回查的 JSON/Markdown 矩阵，不把名称相似当作唯一证据。
- 至少覆盖代表性玩家载具、阶段 Prefab、Module2 家族、ModuleSelection、BonusVehicleStats、升级/神器候选、视觉/VFX/音频挂点。
- 每项标记 `observed`、`inferred` 或 `unknown`，并记录提取来源。
- 为 M0 三条组合各提供一条可借鉴的结构线索和一条不能据此推断的边界。

## After resolution

将矩阵摘要回写到 `docs/assetripper-analysis.md`，再决定 M0 需要哪些原创 `ModuleDef` 字段。

## Answer

已完成 [AssetRipper 引用矩阵](../../assetripper-reference-matrix.md)，并将摘要回写到 `docs/assetripper-analysis.md`。结果确认了模块 Prefab 的阶段层、主动/被动挂点、冷却反馈、车辆阶段和候选预览结构；运行时公式、触发顺序、抽选概率、操控手感和完整音画链仍标为 unknown。三条 M0 组合各有结构线索与边界，下一步进入双功能挂点输入原型。
